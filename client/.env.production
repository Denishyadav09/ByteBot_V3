# Used automatically by `npm run build` (Vite mode = "production").
# Overrides VITE_API_URL only - everything else is inherited from .env.
# Point this at your deployed backend.
VITE_API_URL=https://bytebot-backend.vercel.app
